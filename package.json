{
  "name": "creatorbrand",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
